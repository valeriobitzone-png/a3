# Capability Graph

The graph exposes capabilities and dependency/dataflow relationships to the planner. Discovery may be local, declared, or adapter-backed. Semantic matching is an extension point and is not required for deterministic 0.1 tests.
