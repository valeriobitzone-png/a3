# Adapters

MCP and A2UI are optional.

MCP adapter: MCP tool → A3 Capability; tool invocation → Execution.

A2UI adapter: A3UI state → A2UI surface.

The deterministic A3 core must compile and test without either adapter.
