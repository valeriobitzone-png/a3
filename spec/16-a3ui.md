# A3UI

A3UI is the projection layer. It supports stable component identity, state bindings, semantic gestures, motion, morphing and predictive prefetch. These are presentation concerns and do not alter A3's world-state semantics.
