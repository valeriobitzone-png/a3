<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# A3 Core Model

Every entity has an id, schema version and timestamp where applicable. The core must remain usable with no AI provider.

The canonical cycle is `Intent → Goal → State → Capability → Plan → Policy → Execution → Observation → State`.
