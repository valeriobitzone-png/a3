# Outcome

A3 separates the predicted result from the measured result.

`ExpectedOutcome` belongs to a Plan. `ObservedOutcome` belongs to an Execution and is derived from an Observation. Runtime compares them before committing state.
