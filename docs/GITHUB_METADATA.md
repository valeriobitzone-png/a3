# GitHub metadata

These descriptions and topics are suggestions for manual entry in GitHub repository settings. They are not remote metadata changes.

| Repository | Description | Suggested topics |
|---|---|---|
| `a3` | A3-EP epistemic protocol, Kotlin implementation, A3UI contract, and conformance vectors. | `epistemic-protocol`, `kotlin`, `cloud-events`, `json-canonicalization`, `conformance`, `a3ui` |
| `a3-ts` | TypeScript reference implementation of the A3-EP protocol and lock-vector tests. | `typescript`, `epistemic-protocol`, `cloud-events`, `jcs`, `conformance` |
| `a3-go` | Go reference implementation of A3-EP with canonical envelopes and conformance tests. | `go`, `epistemic-protocol`, `cloud-events`, `jcs`, `conformance` |
| `a3ui-web` | Native Web Components renderer for A3UI surface semantics. | `web-components`, `typescript`, `accessibility`, `a3ui`, `conformance` |
| `a3ui-cli` | Standard-library Python text renderer and protocol test bench for A3UI surfaces. | `python`, `cli`, `a3ui`, `conformance`, `stdlib` |
| `a3ui-graphics` | Renderer-neutral A3UI visual tokens, schemas, and phased graphics specification. | `design-tokens`, `json-schema`, `graphics`, `a3ui`, `accessibility` |

## Badge policy

README badges identify only the repository license, the latest reachable tag, and the local CI workflow. CI badges become live after the corresponding workflow is pushed and its first run completes; they are not claims about a run before publication.
